# Copy ke .env lalu isi API key agar AI benar-benar online.
GEMINI_API_KEY=isi_api_key_gemini_kamu
GEMINI_MODEL=gemini-3.5-flash
PORT=3000
# Optional: isi dengan Render Deploy Hook agar `npm run deploy:render` bisa trigger deploy langsung.
RENDER_DEPLOY_HOOK_URL=
